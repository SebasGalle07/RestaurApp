spring:
  mvc:
    servlet:
      path: /api

  datasource:
    url: jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:6543/postgres?sslmode=require&preferQueryMode=simple
    username: postgres.qxlnxqzfnkrxxxakyvdt
    password: SebasyLisa0809
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc.time_zone: UTC

  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  error:
    include-message: always

app:
  jwt:
    secret: "change-this-secret-to-a-very-long-random-value-32b-min"
    access-ttl-sec: 3600
    refresh-ttl-sec: 1209600
  cors:
    allowed-origins: http://localhost:4200

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
